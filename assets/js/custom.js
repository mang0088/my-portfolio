(function ($) {
  /* ----------------------------------------------------------- */
  /*  FEATURED SLIDE (TYPED SLIDER)
	/* ----------------------------------------------------------- */

  var typed = new Typed('#typed', {
    stringsElement: '#typed-strings',
    typeSpeed: 20,
    backSpeed: 20,
    startDelay: 1000,
    loop: true,
    loopCount: Infinity,
  });
})(jQuery);
